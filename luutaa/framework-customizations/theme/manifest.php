<?php if ( ! defined( 'ABSPATH' ) ) {
	die( 'Forbidden' );
}

$manifest = array();

$manifest['id'] = 'luutaa';

$manifest['supported_extensions'] = array(
	'page-builder' => array(),
	'breadcrumbs' => array(),
	'backups' => array(),
);